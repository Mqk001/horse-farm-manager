'use client';
import { FormEvent, useState } from 'react';
export default function TeamPage() { const [email, setEmail] = useState(''); const [role, setRole] = useState('STAFF'); const [message, setMessage] = useState(''); const [loading, setLoading] = useState(false); async function submit(event: FormEvent) { event.preventDefault(); setLoading(true); setMessage(''); const response = await fetch('/api/invitations', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email, role }) }); const body = await response.json().catch(() => null); setMessage(response.ok ? 'Invitation sent.' : body?.error ?? 'We could not send the invitation.'); setLoading(false); if (response.ok) setEmail(''); } return <main className="page-shell"><header className="mb-10"><p className="eyebrow">Farm workspace</p><h1 className="mt-3 font-display text-4xl text-[#17201a]">Your team</h1><p className="mt-3 max-w-xl text-sm leading-6 text-[#657067]">Invite staff who help care for your horses. Managers can also invite and manage the farm.</p></header><section className="premium-card max-w-xl rounded-3xl p-6 sm:p-8"><h2 className="font-display text-2xl">Invite a teammate</h2><form onSubmit={submit} className="login-form mt-6"><label>Email address<input value={email} onChange={event => setEmail(event.target.value)} type="email" required placeholder="teammate@example.com" /></label><label>Role<select value={role} onChange={event => setRole(event.target.value)}><option value="STAFF">Staff</option><option value="MANAGER">Manager</option></select></label>{message && <p className={message === 'Invitation sent.' ? 'form-success' : 'form-error'}>{message}</p>}<button className="button-primary login-submit" disabled={loading}>{loading ? 'Sending invitation…' : 'Send invitation'}</button></form></section></main>; }
